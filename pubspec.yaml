name: uploader
description: This package is used for creating AAB/APK and IPA files, and sending them to Play Console, TestFlight, and Firebase App Distribution
version: 1.0.0
homepage: https://github.com/emircetn/uploader
repository: https://github.com/emircetn/uploader
issue_tracker: https://github.com/emircetn/uploader/issues

environment:
  sdk: ">=3.9.0 <4.0.0"

executables:
  uploader:

dependencies:
  args: ^2.5.0
  ansicolor: ^2.0.2
  collection: ^1.17.2
  yaml: ^3.1.2
  googleapis_auth: ^2.3.3
  googleapis: ^17.0.0
  dio: ^5.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
